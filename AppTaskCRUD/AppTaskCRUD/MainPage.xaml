<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:AppTaskCRUD.ViewModels"
             xmlns:models="clr-namespace:AppTaskCRUD.Models"
             x:Class="AppTaskCRUD.MainPage"
             x:Name="MainPageDel">

    <ContentPage.BindingContext>
        <viewModels:TaskViewModel/>
    </ContentPage.BindingContext>

    <VerticalStackLayout Padding="20">
        <Entry Placeholder="Enter task title"
               Text="{Binding NewTaskTitle, Mode=TwoWay}"/>
        <Button Text="Add Task"
                Command="{Binding AddTaskCommand}"/>

        <CollectionView ItemsSource="{Binding Tasks}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <HorizontalStackLayout>
                        <CheckBox IsChecked="{Binding IsDone}"
                                CheckedChanged="CheckBox_CheckedChanged"/>
                        <Label Text="{Binding Title}"
                               VerticalOptions="Center"/>
                        <Button Text="🗑️"
                                Command="{Binding BindingContext.DeleteTaskCommand, Source={x:Reference MainPageDel}}"
                                CommandParameter="{Binding .}"
                                BackgroundColor="Transparent"/>
                    </HorizontalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>

</ContentPage>
